---
# Traefik IngressRoute for ArgoCD
# Accessible at https://dev.holm.chat/argoCD/
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`dev.holm.chat`) && PathPrefix(`/argoCD`)
      kind: Rule
      services:
        - name: argocd-server
          port: 443
          scheme: https
          passHostHeader: true
      middlewares:
        - name: argocd-stripprefix
        - name: argocd-headers
---
# Strip /argoCD prefix before forwarding to ArgoCD
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: argocd-stripprefix
  namespace: argocd
spec:
  stripPrefix:
    prefixes:
      - /argoCD
---
# Headers for ArgoCD
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: argocd-headers
  namespace: argocd
spec:
  headers:
    customRequestHeaders:
      X-Forwarded-Proto: https
