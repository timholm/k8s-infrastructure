---
# Traefik IngressRoute for Argo Workflows UI
# Accessible at https://dev.holm.chat/argo-workflows/
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argo-workflows
  namespace: argo
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`dev.holm.chat`) && PathPrefix(`/argo-workflows`)
      kind: Rule
      services:
        - name: argo-server
          port: 2746
      middlewares:
        - name: argo-stripprefix
---
# Strip /argo-workflows prefix before forwarding to Argo Server
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: argo-stripprefix
  namespace: argo
spec:
  stripPrefix:
    prefixes:
      - /argo-workflows
